
TODO
====

Implement the activate methods of RunButton and BuildButton

  And add the top-level HTML substrate for them to render into

  Update the routes to use the new models and views

  Update the CSS to handle the new views

Display nice build numbers, pass/fail, and start time in the numbers list

Fix the missing base path in the directory indices

Tests!

